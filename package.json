{
  "name": "makanuy-consultas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node tools/validate_v6.mjs && wrangler deploy --dry-run",
    "validate": "node tools/validate_v6.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
