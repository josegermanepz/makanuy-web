document.querySelectorAll('[data-api-form]').forEach(form=>form.addEventListener('submit',async e=>{e.preventDefault();const status=form.querySelector('.form-status'),submit=form.querySelector('[type="submit"]');status.textContent='Enviando…';status.className='form-status';submit.disabled=true;try{const data=Object.fromEntries(new FormData(form));data.turnstileToken=window.turnstile?.getResponse()||'';const r=await fetch('/api/forms',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});const out=await r.json();if(!r.ok)throw new Error(out.error||'No se pudo enviar');form.reset();window.turnstile?.reset();status.textContent='Gracias. Recibimos tu información con el folio '+out.folio+'.';status.className='form-status success'}catch(err){status.textContent=err.message;status.className='form-status error'}finally{submit.disabled=false}}));
