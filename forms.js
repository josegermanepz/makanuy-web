document.querySelectorAll('[data-api-form]').forEach(form=>form.addEventListener('submit',async e=>{e.preventDefault();const status=form.querySelector('.form-status');status.textContent='Enviando…';try{const data=Object.fromEntries(new FormData(form));const r=await fetch('/api/forms',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});const out=await r.json();if(!r.ok)throw new Error(out.error||'No se pudo enviar');form.reset();status.textContent='Gracias. Recibimos tu información con el folio '+out.folio+'.';status.className='form-status success'}catch(err){status.textContent=err.message;status.className='form-status error'}}));