import{json,readJson,clean,folio,sendEmail}from'../_shared.js';export const onRequestPost=async({request,env})=>{if(!env.DB)return json({error:'El almacenamiento todavía no está conectado.'},503);const b=await readJson(request),type=b?.type==='referral'?'referral':'corporate',code=folio(type==='referral'?'REF':'EMP');if(type==='referral'&&(!clean(b.name)||!clean(b.email)||!clean(b.referredName)||!clean(b.referredPhone)||!b.consent))return json({error:'Completa todos los campos y confirma el consentimiento.'},400);if(type==='corporate'&&(!clean(b.company)||!clean(b.name)||!clean(b.email)||!clean(b.message)))return json({error:'Completa todos los campos requeridos.'},400);await env.DB.prepare('INSERT INTO form_submissions(id,folio,type,name,email,phone,payload,created_at) VALUES(?,?,?,?,?,?,?,datetime('now'))').bind(crypto.randomUUID(),code,type,clean(b.name,120),clean(b.email,180),clean(b.phone||b.referredPhone,40),JSON.stringify(b)).run();await sendEmail(env,{to:env.BUSINESS_EMAIL||'yunuen.preg@gmail.com',subject:(type==='referral'?'Nuevo referido':'Solicitud empresarial')+' · '+code,html:'<pre>'+JSON.stringify(b,null,2).replaceAll('<','&lt;')+'</pre>'});return json({ok:true,folio:code});};
